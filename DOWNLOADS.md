# Downloads / 下载

> The large installer files are distributed separately and are not stored in this Git repository.
>
> 大体积安装包单独分发，不直接存放在 Git 仓库中。

## macOS Apple Silicon

- **File / 文件：** `VoxCPM-Mac-Installer.zip`
- **Download / 下载：** `TODO: upload the package and replace this line with the public URL`
- **SHA-256：** `TODO`
- **Target / 适用：** Apple Silicon M1 / M2 / M3 / M4

安装方法：

1. 下载并解压。
2. 右键 `Install.command`，选择“打开”。
3. 安装完成后双击 `Start VoxCPM.command`。

## Windows x64

- **File / 文件：** `VoxCPM-Windows-Installer.zip`
- **Download / 下载：** `TODO: upload the package and replace this line with the public URL`
- **SHA-256：** `TODO`
- **Target / 适用：** 64-bit Windows 10/11

安装方法：

1. 下载并解压，不要直接在压缩包内部运行。
2. 双击 `Install.bat`。
3. 安装完成后双击 `%LOCALAPPDATA%\VoxCPM-Studio\Start VoxCPM.bat`。

## Important / 重要说明

- 模型及核心代码来自 [OpenBMB/VoxCPM](https://github.com/OpenBMB/VoxCPM)。
- 安装包体积较大，请核对文件大小和 SHA-256。
- 安装 Python 依赖和 FFmpeg 时可能需要联网。
- 本地生成不使用 Codex、ChatGPT 或 Agent token。
- 请遵守原项目许可证，并负责任地使用声音克隆功能。

